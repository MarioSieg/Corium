# Define variables with type and value:
# The type can be elided here, but we do it explictly 

main () {
	let a int = 3
	let b float = 2.5
	let c char = 'O'
	let d bool = true

	# Reassign the value of the variables:
	a = -4
	b = 3.14
	c = 'X'
	d = false
}
